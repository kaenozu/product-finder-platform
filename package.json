{
  "name": "product-finder-platform",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": "^22.13.0 || ^24.0.0",
    "pnpm": "10.34.5"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-worker.mjs",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "typecheck": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "e2e": "playwright test",
    "db:migrate": "wrangler d1 migrations apply product-finder-platform --local --config wrangler.worker.jsonc",
    "db:rollback": "node scripts/catalog-rollback.mjs",
    "db:reset": "wrangler d1 execute product-finder-platform --local --config wrangler.worker.jsonc --command \"DELETE FROM product_offers; DELETE FROM products; DELETE FROM catalog_versions; DELETE FROM catalog_state; DELETE FROM ingest_runs;\"",
    "deploy:pages": "vite build && node scripts/build-worker.mjs && node scripts/check-kv-binding.mjs && wrangler pages deploy",
    "deploy:cron": "wrangler deploy --config wrangler.cron.jsonc",
    "check:kv": "node scripts/check-kv-binding.mjs",
    "check:deploy": "wrangler deploy --config wrangler.worker.jsonc --dry-run && wrangler deploy --config wrangler.cron.jsonc --dry-run",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm test:integration && pnpm build && git diff --check",
    "verify:ci": "pnpm verify && pnpm audit --audit-level high && pnpm check:deploy && pnpm e2e"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260820.1",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vitejs/plugin-react": "^6.1.0",
    "esbuild": "^0.28.2",
    "eslint": "^10.8.1",
    "eslint-plugin-react-hooks": "^7.0.0",
    "eslint-plugin-react-refresh": "^0.5.4",
    "globals": "^17.11.0",
    "prettier": "^3.9.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0",
    "vite": "^8.0.0",
    "vitest": "^4.0.0",
    "wrangler": "^4.124.0"
  }
}
